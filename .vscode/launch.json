{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Program",
            "program": "${workspaceRoot}/build/server.js",
            "cwd": "${workspaceRoot}",
            "outDir": "${workspaceRoot}/build",
            "sourceMaps": true
        }
    ]
}